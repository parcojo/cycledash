# Copy this file to `secrets.toml` (same dir) and paste your key.
# `secrets.toml` is gitignored — never commit the real key.
# On Streamlit Community Cloud, paste the same line into the app's Secrets box instead.
FRED_API_KEY = "your_32_char_fred_api_key_here"
